---
/**
 * Brand button. Colors come from surface role tokens:
 * primary = Sage bg / Pine text on dark, Forest bg / Bone text on light.
 * Honey structurally cannot become a CTA (no token path leads there).
 */
interface Props {
  variant?: 'primary' | 'secondary';
  href?: string;
  type?: 'button' | 'submit';
  disabled?: boolean;
  /** Append the mono → that shifts on hover (2026-06 design port). */
  arrow?: boolean;
  class?: string;
}

const {
  variant = 'primary',
  href,
  type = 'button',
  disabled = false,
  arrow = false,
  class: className,
} = Astro.props;

const Tag = href ? 'a' : 'button';
---

<Tag
  class:list={['btn', `btn--${variant}`, className]}
  href={href}
  type={href ? undefined : type}
  disabled={href ? undefined : disabled || undefined}
>
  <slot />{arrow && <span class="btn__arrow" aria-hidden="true">&rarr;</span>}
</Tag>

<style>
  .btn {
    display: inline-flex;
    align-items: center;
    gap: var(--space-3);
    font-family: var(--font-sans);
    font-size: var(--text-body-m);
    line-height: 1;
    font-weight: 500;
    letter-spacing: -0.005em;
    padding: var(--space-4) var(--space-8);
    border-radius: var(--radius-full);
    border: 1px solid transparent;
    cursor: pointer;
    text-decoration: none;
    transition:
      background-color var(--dur-fast) var(--ease),
      color var(--dur-fast) var(--ease),
      border-color var(--dur-fast) var(--ease);
  }

  .btn--primary {
    background-color: var(--cta-bg);
    color: var(--cta-fg);
  }

  .btn--primary:hover {
    background-color: color-mix(in srgb, var(--cta-bg) 88%, var(--fg));
    color: var(--cta-fg);
  }

  .btn--secondary {
    background-color: transparent;
    color: var(--fg);
    border-color: color-mix(in srgb, var(--fg) 40%, transparent);
  }

  .btn--secondary:hover {
    border-color: var(--fg);
    color: var(--fg);
  }

  .btn:disabled {
    opacity: 0.45;
    cursor: not-allowed;
  }

  .btn__arrow {
    font-family: var(--font-mono);
    transition: transform var(--dur-base) var(--ease);
  }

  .btn:hover .btn__arrow {
    transform: translateX(3px);
  }
</style>
