# build output
dist/
.astro/

# dependencies
node_modules/

# environment — secrets live in Vercel env vars, never the repo
.env
.env.*

# vercel
.vercel/

# editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Word lock files
~$*

# Illustrator source (21 MB — too heavy for git history; SVG exports live in src/assets)
*.ai

# Heavy source masters (originals kept locally; optimized web assets ship in public/)
_assets-source/

# Design-drop staging — drop zips/files here; sorted output gets committed, the raw drop does not
docs/design/inbox/*
!docs/design/inbox/README.md

# logs
npm-debug.log*
